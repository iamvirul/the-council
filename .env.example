# No API key required — The Council runs inside Claude Code and uses
# the Claude Agent SDK, which inherits Claude Code's session and auth.

# Optional: Log level (default: info)
# Levels: fatal, error, warn, info, debug, trace
LOG_LEVEL=info

# Optional: Node environment
NODE_ENV=development
